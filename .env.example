# ?? NUNCA EXPONGAS ESTE ARCHIVO CON VALORES REALES EN GITHUB
# Este archivo es un TEMPLATE - COPY Y MODIFICA CON TUS VALORES

# === AUTENTICACIÓN JWT ===
# Secreto para firmar tokens JWT (DEBE SER ÚNICO Y SEGURO)
JWT_SECRET=tu_secreto_muy_seguro_aqui_minimo_32_caracteres

# === BASE DE DATOS ORACLE ===
# Usuario de base de datos Oracle
DB_USER=usuario_oracle

# Contraseña del usuario Oracle (NUNCA COMMITS ESTO CON VALORES REALES)
DB_PASSWORD=contraseña_oracle_segura

# String de conexión Oracle (host:puerto/base_datos)
db_CONNECT_STRING=hostname:1521/BDNAME

# === SERVIDOR ===
# Puerto en el que corre el servidor
PORT=5000

# Ambiente (development, production, testing)
NODE_ENV=development

# --- OPCIONAL ---
# Log level (debug, info, warn, error)
LOG_LEVEL=info
